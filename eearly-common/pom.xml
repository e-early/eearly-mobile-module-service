<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>si.result.eearly</groupId>
        <artifactId>eearly-service</artifactId>
        <version>0.1.1-SNAPSHOT</version>
    </parent>

    <artifactId>eearly-common</artifactId>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-context</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-json</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-client</artifactId>
        </dependency>

        <dependency>
            <groupId>org.keycloak</groupId>
            <artifactId>keycloak-admin-client</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-cache</artifactId>
        </dependency>

        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
        </dependency>

        <dependency>
            <groupId>org.mapstruct</groupId>
            <artifactId>mapstruct</artifactId>
        </dependency>

        <dependency>
            <groupId>si.result.r3</groupId>
            <artifactId>r3-rest-filter</artifactId>
        </dependency>

        <dependency>
            <groupId>net.devh</groupId>
            <artifactId>grpc-server-spring-boot-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

<!--        https://stackoverflow.com/a/42389703-->
        <dependency>
            <groupId>com.google.firebase</groupId>
            <artifactId>firebase-admin</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>

        <dependency>
            <groupId>org.ehrbase.openehr.sdk</groupId>
            <artifactId>generator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.ehrbase.openehr.sdk</groupId>
            <artifactId>client</artifactId>
        </dependency>

    </dependencies>

    <build>
        <plugins>
            <!-- version, executions and configuration inherited from the root pom's pluginManagement -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>com.github.os72</groupId>
                <artifactId>protoc-jar-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>

            </plugin>

            <plugin>
                <groupId>org.ehrbase.openehr.sdk</groupId>
                <artifactId>generator-maven-plugin</artifactId>
                <version>${ehrbase.version}</version>
              <executions>
                <execution>
                  <id>generate-openehr-classes</id>
                  <phase>generate-sources</phase>
                  <goals>
                    <goal>codegen</goal>
                  </goals>
                </execution>
              </executions>
                <configuration>
                    <addNullFlavor>true</addNullFlavor>
                    <choicesForSingleEvent>false</choicesForSingleEvent>
                    <optimizerSetting>SECTION</optimizerSetting>
                    <replaceChars>
                        <ö>oe</ö>
                        <ä>ae</ä>
                    </replaceChars>
                    <templateFiles>
                        <templateFile>${project.basedir}/src/main/resources/templates/EEARLY_Report_Blood-glucose.opt</templateFile>
                        <templateFile>${project.basedir}/src/main/resources/templates/EEARLY_Report_Blood-pressure.opt</templateFile>
                        <templateFile>${project.basedir}/src/main/resources/templates/EEARLY_Report_Body-height.opt</templateFile>
                        <templateFile>${project.basedir}/src/main/resources/templates/EEARLY_Report_Body-weight.opt</templateFile>
                        <templateFile>${project.basedir}/src/main/resources/templates/EEARLY_Report_Pulse.opt</templateFile>
                        <templateFile>${project.basedir}/src/main/resources/templates/EEARLY_Report_Pulse-oximetry.opt</templateFile>
                        <templateFile>${project.basedir}/src/main/resources/templates/EEARLY_Report_Body-temperature.opt</templateFile>
                    </templateFiles>
                    <packageName>si.result.eearly.openehr</packageName>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- gRPC's Netty transport needs a native DNS resolver on macOS/Apple Silicon dev
             machines; only pull it in there instead of on every build (Linux CI/prod included). -->
        <profile>
            <id>netty-dns-macos-aarch64</id>
            <activation>
                <os>
                    <family>mac</family>
                    <arch>aarch64</arch>
                </os>
            </activation>
            <dependencies>
                <dependency>
                    <groupId>io.netty</groupId>
                    <artifactId>netty-resolver-dns-native-macos</artifactId>
                    <classifier>osx-aarch_64</classifier>
                    <scope>runtime</scope>
                </dependency>
            </dependencies>
        </profile>
    </profiles>

</project>
